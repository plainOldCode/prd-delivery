# Database configuration (MariaDB, MySQL-compatible)
# Copy this file to .env or .env.local before running the app

DB_HOST=localhost
DB_PORT=3306
DB_NAME=example_app
DB_USERNAME=example
DB_PASSWORD=example
