{
  "name": "prd-delivery-backend",
  "module": "esnext",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --target bun --outdir ./dist",
    "start": "bun run dist/index.js",
    "test": "bun test src/__tests__/",
    "test:api": "PORT=3001 bun test src/__tests__/api.test.ts"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.6.0"
  }
}